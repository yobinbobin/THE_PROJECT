# Learning Path Analyzer

[![Tests](https://github.com/yobinbobin/THE_PROJECT/workflows/Tests%20and%20Code%20Quality/badge.svg)](https://github.com/yobinbobin/THE_PROJECT/actions)

Система анализа пути обучения студента на основе логов LMS (Moodle, Canvas и т.д.). Анализирует, какие типы активностей наиболее эффективны для разных студентов, и визуализирует результаты.

## 🎯 Полезность проекта

Проект решает реальную проблему в области **AI в образовании**:
* **Для преподавателей:** Позволяет выявить, какие активности (форум, тесты, задания) сильнее всего коррелируют с успеваемостью.
* **Для администрации курсов:** Дает данные для оптимизации учебного плана.
* **Для студентов:** Может быть основой для системы персонализированных рекомендаций.

## 📦 Установка

1. **Клонируй репозиторий:**
   ```bash
   git clone https://github.com/yobinbobin/THE_PROJECT.git
   cd THE_PROJECT
   ```
2. **Создай виртуальное окружение и активируй его:**
   ```python -m venv venv
    # Для Linux/macOS:
    source venv/bin/activate
    # Для Windows:
    venv\Scripts\activate
   ```
3. **Установи зависимости:**
```
bash
pip install -r requirements.txt
```

## 🚀 Использование
### Базовый пример
Помести свои логи LMS в формате CSV в папку data/ (например, data/sample_logs.csv) и запусти:
```
bash
python src/main.py
```
Скрипт загрузит данные, проведет анализ, выведет таблицу корреляций и сохранит график в docs/activity_correlation.png.

## Формат входных данных (CSV)
Для работы скрипта нужен CSV файл как минимум со столбцами:

user_id - идентификатор студента

timestamp - дата и время события (например, "2023-10-01 09:00:00")

event_type - тип события. Рекомендуемые значения: login, assignment_submitted, quiz_attempted, forum_posted.

Пример (data/sample_logs.csv):

```csv
user_id,timestamp,event_type
101,2023-10-01 09:00:00,login
101,2023-10-01 14:00:00,quiz_attempted
102,2023-10-02 10:00:00,login
```

## 📊 Пример вывода
После запуска src/main.py ты увидишь в консоли примерно следующее:

```text
=== Learning Path Analyzer ===
Данные загружены. Строк: 1500, Столбцов: 5
Предобработка данных завершена.
Рассчитаны метрики для 120 студентов.

Корреляция активности с оценками:
event_type
assignment_submitted    0.72
quiz_attempted          0.65
login                   0.31
forum_posted            0.28
Name: final_score, dtype: float64
```
А также откроются графики.

## 🏗️ Структура проекта
```text
learning_path_analyzer/
├── src/                       # Исходный код
│   ├── __init__.py
│   ├── data_loader.py        # Загрузка и обработка данных
│   ├── analyzer.py           # Логика анализа
│   ├── visualizer.py         # Построение графиков
│   └── main.py               # Главный скрипт
├── tests/                    # Unit-тесты
│   ├── __init__.py
│   └── test_analyzer.py
├── data/                     # Папка для данных (логов LMS)
│   └── sample_logs.csv      # Пример входных данных
├── docs/                     # Документация и отчеты
├── scripts/                  # Вспомогательные скрипты
├── .github/workflows/        # Конфигурация CI/CD
│   ├── tests.yml            # Workflow с тестами
│   └── scheduled_report.yml # Креативный workflow
├── README.md
├── requirements.txt
└── .gitignore
```

## ✅ Тестирование и CI/CD
Проект использует GitHub Actions для автоматизации:

1. tests.yml: При каждом пуше запускаются линтеры (flake8) и юнит-тесты (pytest). Статус отображается бейджем вверху README.

2. scheduled_report.yml: Креативный workflow, который:

Запускается каждый понедельник в 6:00 UTC (по расписанию cron).

Можно запустить вручную (кнопка workflow_dispatch в Actions).

Генерирует новый отчет, коммитит его в репозиторий и сохраняет как артефакт.
**Запуск тестов локально:**
```
pytest tests/ -v
```